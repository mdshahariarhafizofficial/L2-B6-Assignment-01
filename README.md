# 📘 Blog Post (in Bangla)

## **1. TypeScript এ Interface এবং Type এর মধ্যে পার্থক্য**

TypeScript-এ Interface এবং Type দুটোই ডেটার গঠন (structure) নির্ধারণ করতে ব্যবহৃত হয়। তবে এদের মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য আছে:

### Interface মূলত object-এর structure নির্ধারণ করে  
```ts
interface User {
  name: string;
  age: number;
}
```

### Type আরও flexible  
Type দিয়ে object ছাড়াও primitive, union, tuple, function ইত্যাদি সবকিছু define করা যায়।
```ts
type ID = string | number;
type Point = [number, number];
```

### Interface সহজে extend করা যায়  
```ts
interface Person {
  name: string;
}

interface Employee extends Person {
  salary: number;
}
```

### ❗ Type extend করা যায় — কিন্তু syntax আলাদা  
```ts
type Person = { name: string };
type Employee = Person & { salary: number };
```

### সংক্ষেপে  
- **Interface →** object structure এর জন্য উপযুক্ত  
- **Type →** আরও অনেক ক্ষেত্রে ব্যবহারের জন্য শক্তিশালী এবং flexible  


---

## **2. TypeScript এ any, unknown এবং never এর পার্থক্য**

TypeScript-এ এই তিনটি টাইপ ভিন্ন ভিন্ন উদ্দেশ্যে ব্যবহার করা হয়। প্রতিটা টাইপের আচরণ আলাদা।

---

### 🔹 any — সবচেয়ে free এবং অনিরাপদ টাইপ  
```ts
let value: any = 20;
value = "Hello";
value = true;
```
TypeScript কোনো type-check করবে না। তাই এটি বেশি ব্যবহার করা ঠিক নয়।

---

### 🔹 unknown — any-এর তুলনায় বেশি নিরাপদ  
```ts
let data: unknown = "Hello";

if (typeof data === "string") {
  console.log(data.toUpperCase());
}
```
unknown টাইপে ডেটা ব্যবহার করার আগে অবশ্যই type-check করতে হয়।

---

### 🔹 never — যে function কখনো return করে না  
```ts
function throwError(): never {
  throw new Error("Something went wrong!");
}
```
never তখন ব্যবহার হয় যখন কোনো function বা situation এ return value আসার কথা নয়।

---

### সংক্ষেপে

| Type | ব্যবহার | ব্যাখ্যা |
|------|---------|----------|
| **any** | free, unsafe | যেকোনো ডেটা নিতে পারে, কোনো check নেই |
| **unknown** | safe | আগে type-check ছাড়া ব্যবহার করা যায় না |
| **never** | unreachable | function কখনো value return করবে না |
